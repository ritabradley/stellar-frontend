import Tilt from 'react-parallax-tilt';

const Logo = () => {
    return (
        <Tilt>
            <div>
                <h1 className='text-2xl font-bold m-4 mt-0'>Stellar</h1>
            </div>
        </Tilt>
    );
};

export default Logo;
