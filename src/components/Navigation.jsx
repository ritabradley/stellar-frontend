const Navigation = () => {
    return (
        <nav className='flex justify-end'>
            <p className='text-lg underline p-3 hover:text-gray-400'>Sign Out</p>
        </nav>
    );
};

export default Navigation;
